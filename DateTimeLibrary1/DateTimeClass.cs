namespace DateTimeLibrary
{
    public class DateTimeClass
    {
        private static long FindDifference(DateOnly dateStart, DateOnly dateEnd)
        {
            return 0l;
        }

        private static bool IsLeapYear()
        {
            return true;
        }

        private static DayTimes FindDayTimes(TimeOnly time)
        {
            return DayTimes.Morning;
        }

        private string formatDateTime(DateTime date)
        {
            return "";
        }
    }
}